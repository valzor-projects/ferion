<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 220 60" fill="none">
  <!-- Accent bar -->
  <rect x="0" y="0" width="4" height="60" rx="2" fill="#FF0055"/>
  <!-- YOKOSO wordmark -->
  <text
    x="18"
    y="42"
    font-family="'Outfit', 'Segoe UI', system-ui, sans-serif"
    font-size="36"
    font-weight="700"
    letter-spacing="8"
    fill="#0f172a"
  >YOKOSO</text>
  <!-- Dark theme version is handled via CSS; the SVG works on light bg by default -->
  <!-- Dot accent -->
  <circle cx="212" cy="44" r="4" fill="#FF0055"/>
</svg>
